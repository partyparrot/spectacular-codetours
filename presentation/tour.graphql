query tour($tourRepository: String!) {
  tour(tourRepository: $tourRepository) {
    repository
    targetRepository
    steps {
      title
      slug
      index
      codeUrl
      code
      filePath
      sections {
        slug
        lineStart
        lineEnd
        content
      }
    }
  }
}
