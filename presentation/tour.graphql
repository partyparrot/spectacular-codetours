query tour($tourRepository: String!) {
  tour(tourRepository: $tourRepository) {
    repository
    targetRepository
    steps {
      title
      slug
      code
      sections {
        slug
        lineStart
        lineEnd
        content
      }
    }
  }
}
