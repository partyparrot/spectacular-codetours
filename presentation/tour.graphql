query tour($tourRepository: String!) {
  tour(tourRepository: $tourRepository) {
    _id
    repository
    targetRepository
    description
    steps {
      _id
      title
      slug
      index
      codeUrl
      code
      filePath
      sections {
        slug
        lineStart
        lineEnd
        content
      }
    }
  }
}